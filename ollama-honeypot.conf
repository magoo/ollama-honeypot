[honeypot]
# Host to bind to (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
host = 0.0.0.0

# Port to listen on (default Ollama port is 11434)
port = 11434

# Maximum length of logged content before truncation
max_log_length = 1000
